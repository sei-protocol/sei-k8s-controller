apiVersion: sei.io/v1alpha1
kind: SeiNodeDeployment
spec:
  replicas: 2
  template:
    spec:
      fullNode: {}
      peers:
        - label:
            selector:
              # provision-snd substitutes --chain-id here at apply time.
              sei.io/chain: ""
  updateStrategy:
    type: InPlace
